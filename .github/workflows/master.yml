name: Python JHeaps

on:
  push:
    branches:
      - master

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    container:
      image: dmichail/manylinux:f8ab71a9610b1fdbc7db19773dabd941d57858c2
    steps:
    - name: OS name
      run: |
        uname -a
    - name: Clone repository
      # actions/checkout isn't used here: the container's glibc/libstdc++
      # are too old to run the Node.js runtime that JS-based actions depend
      # on, so the repository is fetched via a manual git clone instead.
      # ($GITHUB_WORKSPACE/../python-jheaps resolves back to $GITHUB_WORKSPACE
      # itself, since the runner's workspace path already ends in
      # .../python-jheaps/python-jheaps.)
      run: |
        git config --global --add safe.directory '*'
        git clone --recursive https://github.com/d-michail/python-jheaps.git $GITHUB_WORKSPACE/../python-jheaps
        git checkout $GITHUB_SHA
    - name: ls GITHUB_WORKSPACE
      run: |
        ls $GITHUB_WORKSPACE
    - name: Build
      run:  $GITHUB_WORKSPACE/.github/scripts/build-manylinux_2_28_x86_64.sh
      shell: bash
    - name: list all wheels
      run: |
        ls $GITHUB_WORKSPACE/dist/*.whl
    - name: upload to PyPi
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
      run:  $GITHUB_WORKSPACE/.github/scripts/upload-pypi.sh
      shell: bash
      env:
        PYPI_USER: __token__
        PYPI_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        #PYPI_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}

  build-non-linux:
    runs-on: ${{ matrix.config.os }}
    strategy:
      fail-fast: true
      matrix:
        config:
        - { os: windows-2022, python-version: "3.10" }
        - { os: windows-2022, python-version: "3.11" }
        - { os: windows-2022, python-version: "3.12" }
        - { os: windows-2022, python-version: "3.13" }
        - { os: windows-2022, python-version: "3.14" }
        - { os: macos-14,     python-version: "3.10" }
        - { os: macos-14,     python-version: "3.11" }
        - { os: macos-14,     python-version: "3.12" }
        - { os: macos-14,     python-version: "3.13" }
        - { os: macos-14,     python-version: "3.14" }
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    - name: Setup python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.config.python-version }}
    - name: Display Python version
      run: |
        python -c "import sys; print(sys.version)"
    - name: Setup GraalVM
      id: setup-graalvm
      uses: graalvm/setup-graalvm@v1
      with:
        java-version: '21'
        distribution: 'graalvm-community'
        github-token: ${{ secrets.GITHUB_TOKEN }}
    - name: Install swig (MacOS)
      if: runner.os == 'MacOS'
      run: |
        brew install swig
    - name: Build Library
      run: |
        pip install wheel
        python setup.py bdist_wheel
    - name: Install and run tests
      run: |
        pip install -r requirements/test.txt
        pip install jheaps --no-index -f dist/
        pytest
    - name: Upload wheel artifacts
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
      uses: actions/upload-artifact@v4
      with:
        name: wheel-${{matrix.config.os}}-${{matrix.config.python-version}}-artifact
        path: dist/
        if-no-files-found: error

  upload-pypi-non-linux:
    needs: build-non-linux
    runs-on: ubuntu-22.04
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
    strategy:
      matrix:
        config:
        - { os: windows-2022, python-version: "3.10" }
        - { os: windows-2022, python-version: "3.11" }
        - { os: windows-2022, python-version: "3.12" }
        - { os: windows-2022, python-version: "3.13" }
        - { os: windows-2022, python-version: "3.14" }
        - { os: macos-14,     python-version: "3.10" }
        - { os: macos-14,     python-version: "3.11" }
        - { os: macos-14,     python-version: "3.12" }
        - { os: macos-14,     python-version: "3.13" }
        - { os: macos-14,     python-version: "3.14" }
    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: wheel-${{matrix.config.os}}-${{matrix.config.python-version}}-artifact
        path: dist
    - name: List files
      run: ls -R
    - name: Upload to PyPi
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        user: __token__
        password: ${{ secrets.PYPI_API_TOKEN }}
        verbose: true
        #password: ${{ secrets.TEST_PYPI_API_TOKEN }}
        #repository_url: https://test.pypi.org/legacy/

